# Read by display managers, and by .xinitrc under startx. It used to restate a
# subset of .profile and had drifted - missing ~/.cargo/bin and the npm path -
# so it defers instead of duplicating.
[ -f "$HOME/.profile" ] && . "$HOME/.profile"
